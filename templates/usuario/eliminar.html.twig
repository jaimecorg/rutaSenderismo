{% extends 'base.html.twig' %}

{% block body %}

    <h1>Eliminar usuario</h1>

    <div class="alert alert-warning">
        ¿Está seguro de que desea eliminar el usuario {{ usuario.correo }}? La operación no se puede deshacer.
    </div>
    <form method="post">
        <a href="{{ path('usuario_modificar', {'id': usuario.id}) }}" class="btn btn-info"><i class="bi bi-box-arrow-up-left"></i> Volver al usuario</a>
        <button type="submit" name="confirmar" value="ok" class="btn btn-danger"><i class="bi bi-trash"></i> Confirmar eliminación</button>
    </form>

{% endblock %}

